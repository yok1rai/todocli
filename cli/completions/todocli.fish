complete -c todocli -f
complete -c todocli -a "add" -d "Add a new todo"
complete -c todocli -a "list" -d "Show all todos"
complete -c todocli -a "done" -d "Mark todo as done"
complete -c todocli -a "delete" -d "Delete a todo"
complete -c todocli -a "clear" -d "Delete all todos"
complete -c todocli -a "help" -d "Show help"
